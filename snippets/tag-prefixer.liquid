{%- if product == blank -%}{%- assign product = item.product -%}{%- endif -%}{%- assign lower_prefix = prefix | downcase -%}{%- if mode == 'cart' -%}  {%- assign tags_array = item.product.tags -%}  {% elsif mode == 'article' %}  {%- assign tags_array = article.tags -%}{%- else -%}  {%- assign tags_array = product.tags -%}{%- endif -%}{%- for tag in tags_array -%}  {% if convert == false %}{%- assign lowercase_tag = tag -%}{%- else -%}{%- assign lowercase_tag = tag | downcase -%}{%- endif -%}{%- if lowercase_tag contains lower_prefix -%}{%- assign tag_to_display = lowercase_tag | remove: lower_prefix -%}{{ tag_to_display }}{%- endif -%}{%- endfor -%}
